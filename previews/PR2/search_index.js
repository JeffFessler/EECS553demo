var documenterSearchIndex = {"docs":
[{"category":"section","location":"generated/examples/k-nn/#knn1","page":"k-NN (k Nearest Neighbor) classifier demo","text":"Illustrate K-nearest neighbors classifier with MNIST hand-written digit images in Julia.\n\nThis page comes from a single Julia file: k-nn.jl.\n\nYou can access the source code for such Julia documentation using the 'Edit on GitHub' link in the top right. You can view the corresponding notebook in nbviewer here: k-nn.ipynb, or open it in binder here: k-nn.ipynb.","title":"k-NN (k Nearest Neighbor) classifier demo"},{"category":"section","location":"generated/examples/k-nn/#Setup","page":"k-NN (k Nearest Neighbor) classifier demo","text":"Add the Julia packages used in this demo. Change false to true in the following code block if you are using any of the following packages for the first time.\n\nif false\n    import Pkg\n    Pkg.add([\n        \"InteractiveUtils\"\n        \"GeometryBasics\"\n        \"LaTeXStrings\"\n        \"LinearAlgebra\"\n        \"MIRTjim\"\n        \"MLDatasets\"\n        \"NearestNeighbors\"\n        \"Plots\"\n        \"VoronoiCells\"\n    ])\nend\n\nTell Julia to use the following packages. Run Pkg.add() in the preceding code block first, if needed.\n\nusing GeometryBasics: Point2\nusing InteractiveUtils: versioninfo\nusing LaTeXStrings\nusing LinearAlgebra: svd\nusing MIRTjim: jim, prompt\nusing MLDatasets: MNIST\nusing NearestNeighbors: KDTree, knn\nusing Plots: default, gui, savefig, plot, plot!, scatter!, RGB, cgrad\nusing Random: randperm, seed!\nusing VoronoiCells: voronoicells, Rectangle\ndefault(); default(markersize=3, markerstrokecolor=:auto, label=\"\",\n tickfontsize=14, labelfontsize=18, legendfontsize=18, titlefontsize=18)\n\nThe following line is helpful when running this file as a script; this way it will prompt user to hit a key after each figure is displayed.\n\nisinteractive() ? jim(:prompt, true) : prompt(:draw);","title":"Setup"},{"category":"section","location":"generated/examples/k-nn/#Load-data","page":"k-NN (k Nearest Neighbor) classifier demo","text":"Read the MNIST data for some handwritten digits. This code will automatically download the data from web if needed and put it in a folder like: ~/.julia/datadeps/MNIST/.\n\nif !@isdefined(data) || true\n    digitn = [4,7] # which digits to use\n    isinteractive() || (ENV[\"DATADEPS_ALWAYS_ACCEPT\"] = true) # avoid prompt\n    dataset = MNIST(Float32, :train)\n    nrep = 1000 # how many of each digit\n    # function to extract the 1st `nrep` examples of digit n:\n    data = n -> dataset.features[:,:,findall(==(n), dataset.targets)[1:nrep]]\n    data = cat(dims=4, data.(digitn)...)\n    labels = vcat([fill(d, nrep) for d in digitn]...) # to check later\n    nx, ny, nrep, ndigit = size(data)\n    data = data[:,2:ny,:,:] # make images non-square to force debug\n    ny = size(data,2)\n    size(data) # (nx, ny, nrep, ndigit)\nend\n(28, 27, 1000, 2)\n\nLook at some of the image data\n\npd = jim(data[:,:,1:10,:], \"Data, d=$(nx*ny), N=$(nrep*ndigit)\";\n    colorbar=nothing, size=(600,200), tickfontsize=6, ncol=10)\n\n# savefig(pd, \"knn-digit-47.pdf\")\nPlot{Plots.GRBackend() n=21}\nCaptured extra kwargs:\n  Series{1}:\n    transpose: false\n\n\nPartition data into train / validate / test\n\nntrain = 200\nnvalid = 100\nntest1 = 600\nseed!(0)\ntmp = randperm(nrep)\nitrain = (1:ntrain)\nivalid = (1:nvalid) .+ ntrain\nitest1 = (1:ntest1) .+ (ntrain + nvalid)\ndtrain = data[:,:,tmp[itrain],:]\ndvalid = data[:,:,tmp[ivalid],:]\ndtest1 = data[:,:,tmp[itest1],:];\n\nmean training image\n\ndmean = sum(dtrain, dims = 3:4) / ntrain / ndigit\npm = jim(dmean; title=\"Mean image\")\n\n# savefig(pm, \"knn-mean.pdf\")\nPlot{Plots.GRBackend() n=1}\nCaptured extra kwargs:\n  Series{1}:\n    transpose: false\n","title":"Load data"},{"category":"section","location":"generated/examples/k-nn/#PCA-based-dimensionality-reduction","page":"k-NN (k Nearest Neighbor) classifier demo","text":"Use two components for easy visualization\n\nX = reshape(dtrain .- dmean, nx*ny, :) # unfold\nK = 2\nU = svd(X).U[:,1:K];\n\nShow basis vectors\n\ntmp = reshape(U, nx, ny, K)\npu = jim(tmp; title=\"Basis functions, K=$K\", color=:cividis, size=(700,400))\n\n# savefig(pu, \"knn-u.pdf\")\nPlot{Plots.GRBackend() n=3}\nCaptured extra kwargs:\n  Series{1}:\n    transpose: false\n\n\nprompt()\n\nif false # use small data sample for Voronoi plots only\n    ntrain = 15\n    dtrain = dtrain[:,:,1:ntrain,:]\nend","title":"PCA-based dimensionality reduction"},{"category":"section","location":"generated/examples/k-nn/#Visualize-embedded-data","page":"k-NN (k Nearest Neighbor) classifier demo","text":"embed(data, n) = reshape(U' * reshape(data .- dmean, nx*ny, :), K, n, :)\nXtrain = embed(dtrain, ntrain) # (K, ntrain, ndigit)\nXvalid = embed(dvalid, nvalid)\nXtest1 = embed(dtest1, ntest1)\nlabeler(n) = vcat([fill(digitn[id], n) for id in 1:ndigit]...)\nytrain = labeler(ntrain)\nyvalid = labeler(nvalid)\nytest1 = labeler(ntest1)\n\nargs = (;\n xaxis = (L\"x_1\", (-1,1) .* 6, -4:2:4),\n yaxis = (L\"x_2\", (-1,1) .* 6, -4:2:4),\n aspect_ratio = 1,\n size = (550, 500),\n)\n\npetr = plot(; title=\"Train data\", args...)\npete = plot(; title=\"Test data\", args...)\n\ncolors = (:blue, :red)\nfor id in 1:ndigit\n    scatter!(petr, Xtrain[1,:,id], Xtrain[2,:,id], label=\"$(digitn[id])\",\n      color = colors[id])\n    scatter!(pete, Xtest1[1,:,id], Xtest1[2,:,id], label=\"$(digitn[id])\",\n      color = colors[id])\nend\npp = plot(petr, pete; size = (950, 500))\n\n# savefig(pp, \"knn-data.pdf\")\nPlot{Plots.GRBackend() n=4}\n\nprompt()","title":"Visualize embedded data"},{"category":"section","location":"generated/examples/k-nn/#Construct-k-NN-classifier","page":"k-NN (k Nearest Neighbor) classifier demo","text":"data_tmp = reshape(Xtrain, K, ntrain*ndigit) # (K, ntrain)\ntree = KDTree(data_tmp)\n\ni2label = i -> getindex(ytrain, i)\nfunction knn_class(point::AbstractVector, k::Int)\n    idx, _ = knn(tree, point, k)\n    labels = sort(i2label.(idx))\n    return labels[k÷2+1] # majority vote\nend\n\nx1_range = range(-6, 6, 221)\nx2_range = range(-6, 6, 223)\n\nα = 0.2\ncolor = cgrad([RGB(1-α, 1-α, 1), :black, RGB(1, 1-α, 1-α)])\nfunction knn_plot(k::Int, error)\n    error = round(error; sigdigits=2)\n    tmp = [knn_class([x1; x2], k) for x1 in x1_range, x2 in x2_range]\n    p = jim(x1_range, x2_range, tmp; color,\n            title = \"k=$k, train error=$error %\",\n            prompt = false, args...,\n        )\n    for id in 1:ndigit\n        scatter!(p, Xtrain[1,:,id], Xtrain[2,:,id],\n            color = colors[id],\n            label = \"$(digitn[id])\",\n        )\n    end\n    return p\nend;","title":"Construct k-NN classifier"},{"category":"section","location":"generated/examples/k-nn/#Classification-errors","page":"k-NN (k Nearest Neighbor) classifier demo","text":"for train / validate / test\n\nklist = 1:2:min(30, ntrain)\nfunction errors(data, label, klist)\n    data = reshape(data, K, :) # (d, n)\n    return [count(knn_class.(eachcol(data), k) .!= label) for k in klist] /\n        size(data, 2) * 100\nend\ntrain_error = errors(Xtrain, ytrain, klist)\nvalid_error = errors(Xvalid, yvalid, klist)\ntest1_error = errors(Xtest1, ytest1, klist);\n\nFunction to add Voronoi cells to a k-NN plot\n\nfunction add_voronoi!(pp, X; xmax = 6)\n    tmp = eachcol(reshape(Float64.(X), K, :))\n    points = Point2.(tmp)\n\n    # Bounding box to clip the infinite outer cells\n    rect = Rectangle(Point2(-1, -1)*xmax, Point2(1, 1)*xmax)\n    tessellation = voronoicells(points, rect)\n\n    for (i, cell) in enumerate(tessellation.Cells)\n        # Extract x and y coordinates of the cell vertices\n        x_coords = [vertex[1] for vertex in cell]\n        y_coords = [vertex[2] for vertex in cell]\n\n        # Close the polygon by repeating the first vertex\n        push!(x_coords, x_coords[1])\n        push!(y_coords, y_coords[1])\n\n        # Draw the cell polygon\n        plot!(pp, x_coords, y_coords, linecolor = :black, linewidth = 0.5,)\n    end\n    return pp\nend\nadd_voronoi! (generic function with 1 method)","title":"Classification errors"},{"category":"section","location":"generated/examples/k-nn/#Decision-regions-for-various-k","page":"k-NN (k Nearest Neighbor) classifier demo","text":"p1 = knn_plot(1, train_error[1])\n# savefig(p1, \"knn-k=1.pdf\")\n\n# Voronoi plots per 553 student question\nif ntrain == 15 # only for 553 figure\n    add_voronoi!(p1, Xtrain)\n    # savefig(p1, \"voronoi-k=1.pdf\")\n\n    p3 = knn_plot(3, train_error[only(findall(==(3), klist))])\n    add_voronoi!(p3, Xtrain)\n    # savefig(p3, \"voronoi-k=3.pdf\")\nend\n\np5 = knn_plot(5, train_error[only(findall(==(5), klist))])\n# savefig(p5, \"knn-k=5.pdf\")\nPlot{Plots.GRBackend() n=3}\nCaptured extra kwargs:\n  Series{1}:\n    transpose: false\n\n\np23 = knn_plot(23, train_error[only(findall(==(23), klist))])\n# savefig(p23, \"knn-k=23.pdf\")\nPlot{Plots.GRBackend() n=3}\nCaptured extra kwargs:\n  Series{1}:\n    transpose: false\n\n\np99 = knn_plot(99, only(errors(Xtrain, ytrain, [99])))\n# savefig(p99, \"knn-k=99.pdf\")\nPlot{Plots.GRBackend() n=3}\nCaptured extra kwargs:\n  Series{1}:\n    transpose: false\n","title":"Decision regions for various k"},{"category":"section","location":"generated/examples/k-nn/#Plot-error-rates","page":"k-NN (k Nearest Neighbor) classifier demo","text":"k_valid = klist[argmin(valid_error)] # best k for validation data\nk_test1 = klist[argmin(test1_error)] # best k for test data\ndefault(markersize = 5)\npe = plot(\n xaxis = (L\"k\", (1,klist[end]), [1, k_valid, k_test1, klist[end]]),\n yaxis = (\"Error (%)\", ),\n widen = true,\n)\nplot!(klist, valid_error, marker=:downtri, label=\"Validation\")\nplot!(klist, test1_error, marker=:square, label=\"Test\")\nplot!(klist, train_error, marker=:o, label=\"Train\")\nPlot{Plots.GRBackend() n=3}\n\n# savefig(pe, \"knn-error.pdf\")\n\n\ninclude(\"../../../inc/reproduce.jl\")\n\u001b[32m\u001b[1mStatus\u001b[22m\u001b[39m `~/work/EECS553demo/EECS553demo/docs/Project.toml`\n  \u001b[90m[47edcb42] \u001b[39mADTypes v1.24.0\n  \u001b[90m[e30172f5] \u001b[39mDocumenter v1.19.0\n  \u001b[90m[2c06267c] \u001b[39mEECS553demo v0.0.1 `~/work/EECS553demo/EECS553demo`\n\u001b[33m⌅\u001b[39m \u001b[90m[5c1252a2] \u001b[39mGeometryBasics v0.4.11\n  \u001b[90m[b964fa9f] \u001b[39mLaTeXStrings v1.4.1\n  \u001b[90m[98b081ad] \u001b[39mLiterate v2.21.0\n  \u001b[90m[7035ae7a] \u001b[39mMIRT v0.18.3\n  \u001b[90m[170b2178] \u001b[39mMIRTjim v0.26.0\n  \u001b[90m[eb30cadb] \u001b[39mMLDatasets v0.7.21\n  \u001b[90m[b8a86587] \u001b[39mNearestNeighbors v0.4.29\n  \u001b[90m[429524aa] \u001b[39mOptim v2.3.1\n\u001b[32m⌃\u001b[39m \u001b[90m[91a5bcdd] \u001b[39mPlots v1.41.4\n  \u001b[90m[10745b16] \u001b[39mStatistics v1.11.5\n  \u001b[90m[e3e34ffb] \u001b[39mVoronoiCells v0.4.0\n  \u001b[90m[b77e0a4c] \u001b[39mInteractiveUtils v1.11.0\n  \u001b[90m[37e2e46d] \u001b[39mLinearAlgebra v1.12.0\n  \u001b[90m[44cfe95a] \u001b[39mPkg v1.12.1\n  \u001b[90m[9a3f8284] \u001b[39mRandom v1.11.0\n\u001b[36m\u001b[1mInfo\u001b[22m\u001b[39m Packages marked with \u001b[32m⌃\u001b[39m and \u001b[33m⌅\u001b[39m have new versions available. Those with \u001b[32m⌃\u001b[39m may be upgradable, but those with \u001b[33m⌅\u001b[39m are restricted by compatibility constraints from upgrading. To see why use `status --outdated`\n\n\n\nThis page was generated using Literate.jl.","title":"Plot error rates"},{"category":"section","location":"generated/examples/decision-boundary/#decision-boundary","page":"Decision boundaries","text":"Illustrate decision boundaries for binary classification in Julia.\n\nThis page comes from a single Julia file: decision-boundary.jl.\n\nYou can access the source code for such Julia documentation using the 'Edit on GitHub' link in the top right. You can view the corresponding notebook in nbviewer here: decision-boundary.ipynb, or open it in binder here: decision-boundary.ipynb.","title":"Decision boundaries"},{"category":"section","location":"generated/examples/decision-boundary/#Setup","page":"Decision boundaries","text":"Add the Julia packages used in this demo. Change false to true in the following code block if you are using any of the following packages for the first time.\n\nif false\n    import Pkg\n    Pkg.add([\n        \"InteractiveUtils\"\n        \"LaTeXStrings\"\n        \"MIRTjim\"\n        \"Plots\"\n        \"Random\"\n        \"Statistics\"\n    ])\nend\n\nTell Julia to use the following packages. Run Pkg.add() in the preceding code block first, if needed.\n\nusing InteractiveUtils: versioninfo\nusing LaTeXStrings\nusing MIRTjim: jim, prompt\nusing Plots: default, gui, savefig, RGB, cgrad\nusing Plots: histogram!, plot, plot!, scatter, scatter!\nusing Random: seed!\nusing Statistics: mean\ndefault(); default(markersize=6, linewidth=2, markerstrokecolor=:auto, label=\"\",\n markerstrokewidth = 2,\n tickfontsize=12, labelfontsize=14, legendfontsize=12, titlefontsize=16)\n\nThe following line is helpful when running this file as a script; this way it will prompt user to hit a key after each figure is displayed.\n\nisinteractive() ? prompt(:prompt) : prompt(:draw);","title":"Setup"},{"category":"section","location":"generated/examples/decision-boundary/#1D-case","page":"Decision boundaries","text":"seed!(4)\nn1 = 20\nn2 = 21\nn = n1 + n2\nX = [2 .+ 1.5 * randn(n1); 6 .+ 1.5 * randn(n2)]\ny = [fill(-1, n1); fill(+1, n2)]\nX1 = X[y .== +1]\nX2 = X[y .== -1]\ndmu = mean(X1) - mean(X2)\n\np1 = plot(\n xaxis = (L\"x\", (-1, 9), 0:4:8),\n yaxis = (L\"f(x)\", (-3, 3), -1:1),\n legend = :bottomright,\n)\nscatter!(X2, 0*X2 .- 0.05, label=\"-1\", marker=:circle, alpha=1, color=:red)\nscatter!(X1, 0*X1 .+ 0.05, label=\"+1\", marker=:+, color=:blue)\nx = [-1, dmu-10eps(), dmu+10eps(), 9]\nw = 1\nb = -dmu\nftil(x) = w * x + b\nf(x) = sign(ftil(x))\nplot!(x, ftil.(x), label=L\"\\tilde{f}(x)\\quad \\mathrm{(affine)}\")\nplot!(x, f.(x), label=L\"f(x) = \\mathrm{sign}(\\tilde{f}(x))\")\n\n# savefig(p1, \"decision1.pdf\")\nPlot{Plots.GRBackend() n=4}\n\nprompt()","title":"1D case"},{"category":"section","location":"generated/examples/decision-boundary/#2D-case","page":"Decision boundaries","text":"Generate synthetic data from two classes\n\nif !@isdefined(v1) || true\n    seed!(0)\n    n0 = 90\n    n1 = 80\n    mu0 = [1, 6]\n    mu1 = [5, 4]\n    v0 = mu0 .+ 1*randn(2,n0) # class -1\n    v1 = mu1 .+ 1*randn(2,n1) # class 1\n    nex = 0\n    v0 = [v0; ones(1,n0)] # (3, n0) training data - with intercept\n    v1 = [v1; ones(1,n1)] # (3, n1) training data - with intercept\nend;\n\nColor map for discriminant function\n\nRGB255(args...) = RGB((args ./ 255)...)\ncolor = cgrad([RGB255(255, 0, 0), :black, RGB255(0, 0, 255)]);\nx1 = range(-2, 7, 91)\nx2 = range(1, 9, 81);\n\nScatter plot and decision boundary\n\nfunction fplot(ff::Matrix, title::AbstractString)\n    v1p = range(-2, 7, 3)\n\n    ps = jim(x1, x2, ff; title,\n     color, alpha = 0.5, yflip = false,\n     xaxis = (L\"x_1\", (-2, 7), 0:3:6),\n     yaxis = (L\"x_2\", (1, 9), 1:3:8),\n     size = (550, 500), legend = :bottomleft,\n     colorbar_ticks = -1:1,\n    )\n\n    plot!(v1p, 2*v1p .- 1.5, color=:magenta, label=\"boundary\")\n    scatter!(v0[1,:], v0[2,:]; color = :red, label=\"-1\")\n    scatter!(v1[1,:], v1[2,:]; color = :blue, label=\"+1\", marker = :+)\nend\n\nftil2 = x1 .- 0.5*x2' .- 0.75 # w'x + b\nffun2 = sign.(ftil2)\np2ftil = fplot(ftil2, L\"\\tilde{f}(\\mathbf{x})\")\np2ffun = fplot(ffun2, L\"f(\\mathbf{x})\")\np2 = plot(p2ftil, p2ffun, size=(950,400))\nPlot{Plots.GRBackend() n=8}\nCaptured extra kwargs:\n  Series{1}:\n    transpose: false\n  Series{5}:\n    transpose: false\n\n\nEither f(x) or tildef(x) is called the discriminant function, depending on the literature.\n\n# savefig(p2, \"decision2.pdf\")\n\n\n\nThis page was generated using Literate.jl.","title":"2D case"},{"category":"section","location":"generated/examples/lda47/#lda47","page":"LDA demo","text":"Illustrate Linear discriminant analysis (LDA) with MNIST hand-written digit images in Julia.\n\nThis page comes from a single Julia file: lda47.jl.\n\nYou can access the source code for such Julia documentation using the 'Edit on GitHub' link in the top right. You can view the corresponding notebook in nbviewer here: lda47.ipynb, or open it in binder here: lda47.ipynb.","title":"LDA demo"},{"category":"section","location":"generated/examples/lda47/#Setup","page":"LDA demo","text":"Add the Julia packages used in this demo. Change false to true in the following code block if you are using any of the following packages for the first time.\n\nif false\n    import Pkg\n    Pkg.add([\n        \"InteractiveUtils\"\n        \"LaTeXStrings\"\n        \"LinearAlgebra\"\n        \"MIRTjim\"\n        \"MLDatasets\"\n        \"Plots\"\n        \"Statistics\"\n    ])\nend\n\nTell Julia to use the following packages. Run Pkg.add() in the preceding code block first, if needed.\n\nusing InteractiveUtils: versioninfo\nusing LaTeXStrings: @L_str, latexstring\nusing LinearAlgebra: svd\nusing MIRTjim: jim, prompt\nusing MLDatasets: MNIST\nusing Plots: default, gui, savefig, plot, plot!, scatter!, RGB, cgrad\nusing Random: randperm, seed!\nusing Statistics: mean\ndefault(); default(markersize=3, markerstrokecolor=:auto, label=\"\",\n tickfontsize=14, labelfontsize=18, legendfontsize=18, titlefontsize=18)\n\nThe following line is helpful when running this file as a script; this way it will prompt user to hit a key after each figure is displayed.\n\nisinteractive() ? jim(:prompt, true) : prompt(:draw);","title":"Setup"},{"category":"section","location":"generated/examples/lda47/#Load-data","page":"LDA demo","text":"Read the MNIST data for some handwritten digits. This code will automatically download the data from web if needed and put it in a folder like: ~/.julia/datadeps/MNIST/.\n\nif !@isdefined(data) || true\n    digitn = [4,7] # which digits to use\n    digitn = [1,7] # which digits to use\n    isinteractive() || (ENV[\"DATADEPS_ALWAYS_ACCEPT\"] = true) # avoid prompt\n    dataset = MNIST(Float32, :train)\n    nrep = 1000 # how many of each digit\n    # function to extract the 1st `nrep` examples of digit n:\n    data = n -> dataset.features[:,:,findall(==(n), dataset.targets)[1:nrep]]\n    data = cat(dims=4, data.(digitn)...)\n    labels = vcat([fill(d, nrep) for d in digitn]...) # to check later\n    nx, ny, nrep, ndigit = size(data)\n    data = data[:,2:ny,:,:] # make images non-square to force debug\n    ny = size(data,2)\n    size(data) # (nx, ny, nrep, ndigit)\nend\n(28, 27, 1000, 2)\n\nLook at some of the image data\n\npd = jim(data[:,:,1:10,:], \"Data, d=$(nx*ny), N=$(nrep*ndigit)\";\n    colorbar=nothing, size=(600,200), tickfontsize=6, ncol=10)\n\ndigit_str = prod(\"$d\" for d in digitn); # string for file names\n# savefig(pd, \"lda$digit_str-digit.pdf\")\n\"17\"\n\nPartition data into train / validate / test\n\nntrain = 200\nnvalid = 100\nntest1 = 600\nseed!(0)\ntmp = randperm(nrep)\nitrain = (1:ntrain)\nivalid = (1:nvalid) .+ ntrain\nitest1 = (1:ntest1) .+ (ntrain + nvalid)\ndtrain = data[:,:,tmp[itrain],:]\ndvalid = data[:,:,tmp[ivalid],:]\ndtest1 = data[:,:,tmp[itest1],:];\n\nmean training image\n\ndmean = sum(dtrain, dims = 3:4) / ntrain / ndigit\npm = jim(dmean; title=\"Mean image\")\n\n# savefig(pm, \"lda$digit_str-mean.pdf\")\nPlot{Plots.GRBackend() n=1}\nCaptured extra kwargs:\n  Series{1}:\n    transpose: false\n","title":"Load data"},{"category":"section","location":"generated/examples/lda47/#PCA-based-dimensionality-reduction","page":"LDA demo","text":"Use two components for easy visualization\n\nX = reshape(dtrain .- dmean, nx*ny, :) # unfold\nK = 2\nU = svd(X).U[:,1:K];\n\nShow basis vectors\n\ntmp = reshape(U, nx, ny, K)\npu = jim(tmp; title=\"Basis functions, K=$K\", color=:cividis, size=(700,400))\n\n# savefig(pu, \"lda$digit_str-u.pdf\")\nPlot{Plots.GRBackend() n=3}\nCaptured extra kwargs:\n  Series{1}:\n    transpose: false\n","title":"PCA-based dimensionality reduction"},{"category":"section","location":"generated/examples/lda47/#Visualize-embedded-data","page":"LDA demo","text":"embed(data, n) = reshape(U' * reshape(data .- dmean, nx*ny, :), K, n, :)\nXtrain = embed(dtrain, ntrain) # (K, ntrain, ndigit)\nXvalid = embed(dvalid, nvalid)\nXtest1 = embed(dtest1, ntest1)\nlabeler(n) = vcat([fill(digitn[id], n) for id in 1:ndigit]...)\nytrain = labeler(ntrain)\nyvalid = labeler(nvalid)\nytest1 = labeler(ntest1)\n\nargs = (;\n xaxis = (L\"x_1\", (-1,1) .* 6, -4:2:4),\n yaxis = (L\"x_2\", (-1,1) .* 6, -4:2:4),\n aspect_ratio = 1,\n size = (550, 500),\n)\n\npetr = plot(; title=\"Train data\", args...)\npete = plot(; title=\"Test data\", args...)\n\ncolors = (:blue, :red)\nfor id in 1:ndigit\n    scatter!(petr, Xtrain[1,:,id], Xtrain[2,:,id], label=\"$(digitn[id])\",\n      color = colors[id])\n    scatter!(pete, Xtest1[1,:,id], Xtest1[2,:,id], label=\"$(digitn[id])\",\n      color = colors[id])\nend\npp = plot(petr, pete; size = (950, 500))\n\n# savefig(pp, \"lda$digit_str-data.pdf\")\nPlot{Plots.GRBackend() n=4}\n\nprompt()","title":"Visualize embedded data"},{"category":"section","location":"generated/examples/lda47/#Illustrate-maximum-likelihood-estimates","page":"LDA demo","text":"Means\n\nmeans = [vec(mean(X; dims=2)) for X in eachslice(Xtrain, dims=3)]\nfunction plot_means!(p; dolabel::Bool = true)\n    for id in 1:ndigit\n        scatter!(p, [means[id][1]], [means[id][2]]; color = colors[id],\n            markerstrokecolor = :black, marker = :square, markersize = 7,\n            label = dolabel ? latexstring(\"\\\\mathbf{μ}_$(digitn[id])\") : \"\",\n        )\n    end\n    return p\nend\nplot_means!(petr)\nplot!(petr, title = \"Train data with sample means\")\npetr\nPlot{Plots.GRBackend() n=4}\n\nprompt()\n\n# savefig(petr, \"lda$digit_str-means.pdf\")\n\n\n# Plot de-meaned data\nXdemean = cat([Xtrain[:,:,id] .- means[id] for id in 1:ndigit]..., dims=3)\n\npdm = plot(; title=\"Train data de-meaned\", args...)\nfor id in 1:ndigit\n    scatter!(pdm, Xdemean[1,:,id], Xdemean[2,:,id], color = colors[id],\n        label = \"$(digitn[id])\",\n    )\nend\npdm\n\n\n# Plot covariance ellipsoid\n\ntmp = reshape(Xdemean, K, ntrain*ndigit) # pool all data\nΣ = tmp * tmp' / size(tmp,2) # sample covariance\n\nz = hcat([collect(sincos(t)) for t in range(0, 2π, 101)]...)\nxc = sqrt(2Σ) * z\npc = deepcopy(pdm)\nplot!(pc, xc[1,:], xc[2,:], color=:black, label=\"½ x'Σ⁻¹x = 1\")\nplot!(pc, legend = :topleft, legendfontsize = 12)\n\n# savefig(pc, \"lda$digit_str-cov.pdf\")\n\nprompt()","title":"Illustrate maximum-likelihood estimates"},{"category":"section","location":"generated/examples/lda47/#LDA-classifier","page":"LDA demo","text":"LDA classifier v1: brute-force way\n\nsqrtΣinv = inv(sqrt(Σ))\nfunction lda_classify1(\n    x::AbstractVector;\n    means::Vector{<:Vector} = means,\n    sqrtΣinv::AbstractMatrix = sqrtΣinv,\n    prob::AbstractVector = fill(0.5, length(means)),\n)\n    score = copy(prob)\n    for id in 1:length(means)\n        r = sqrtΣinv * (x - means[id]) # whitened residual\n        score[id] *= exp.(-(1/2) * sum(abs2, r))\n    end\n    return digitn[argmax(score)]\nend\n\n\nα = 0.2\ncolor = cgrad([RGB(1-α, 1-α, 1), :black, RGB(1, 1-α, 1-α)])\nx1_range = range(-6, 6, 221)\nx2_range = range(-6, 6, 223)\nfunction lda_plot(error::Real)\n    error = round(error; sigdigits=2)\n    tmp = [lda_classify1([x1; x2]) for x1 in x1_range, x2 in x2_range]\n    p = jim(x1_range, x2_range, tmp; color,\n            title = \"LDA train error=$error %\",\n            prompt = false, args...,\n        )\n    for id in 1:ndigit\n        scatter!(p, Xtrain[1,:,id], Xtrain[2,:,id],\n            color = colors[id],\n            label = \"$(digitn[id])\",\n        )\n        plot!(p, means[id][1] .+ xc[1,:], means[id][2] .+ xc[2,:],\n            color = :black,)\n    end\n    plot_means!(p, dolabel = false)\n    return p\nend;","title":"LDA classifier"},{"category":"section","location":"generated/examples/lda47/#Classification-errors","page":"LDA demo","text":"for train / validate / test\n\nfunction errors(data, label)\n    data = reshape(data, K, :) # (d, n)\n    return 100 * count(lda_classify1.(eachcol(data)) .!= label) / size(data, 2)\nend\ntrain_error = errors(Xtrain, ytrain)\nvalid_error = errors(Xvalid, yvalid)\ntest1_error = errors(Xtest1, ytest1)\n[ train_error valid_error test1_error ]\n\np0 = lda_plot(train_error)\n# savefig(p0, \"lda$digit_str-v1.pdf\")\nPlot{Plots.GRBackend() n=7}\nCaptured extra kwargs:\n  Series{1}:\n    transpose: false\n\n\n\n\nThis page was generated using Literate.jl.","title":"Classification errors"},{"category":"section","location":"generated/examples/logistic-sparse/#logistic-sparse","page":"Sparse logistic regression with POGM","text":"Binary classification via sparse (using 1-norm) logistic regression in Julia.\n\nThis page comes from a single Julia file: logistic-sparse.jl.\n\nYou can access the source code for such Julia documentation using the 'Edit on GitHub' link in the top right. You can view the corresponding notebook in nbviewer here: logistic-sparse.ipynb, or open it in binder here: logistic-sparse.ipynb.","title":"Sparse logistic regression with POGM"},{"category":"section","location":"generated/examples/logistic-sparse/#Setup","page":"Sparse logistic regression with POGM","text":"Add the Julia packages used in this demo. Change false to true in the following code block if you are using any of the following packages for the first time.\n\nif false\n    import Pkg\n    Pkg.add([\n        \"ADTypes\"\n        \"InteractiveUtils\"\n        \"LaTeXStrings\"\n        \"LinearAlgebra\"\n        \"MIRTjim\"\n        \"MIRT\"\n        \"Optim\"\n        \"Plots\"\n        \"Random\"\n        \"Statistics\"\n    ])\nend\n\nTell Julia to use the following packages. Run Pkg.add() in the preceding code block first, if needed.\n\nusing InteractiveUtils: versioninfo\nusing LaTeXStrings\nusing LinearAlgebra: dot, eigvals\nusing MIRTjim: prompt\nusing MIRT: pogm_restart\nusing Optim: optimize\nimport Optim # Options\nusing Plots: default, gui, savefig, twinx\nusing Plots: histogram!, plot, plot!, scatter, scatter!\nusing Random: seed!\nusing Statistics: mean\ndefault(); default(markersize=6, linewidth=2, markerstrokecolor=:auto, label=\"\",\n tickfontsize=12, labelfontsize=14, legendfontsize=12, titlefontsize=16)\n\nThe following line is helpful when running this file as a script; this way it will prompt user to hit a key after each figure is displayed.\n\nisinteractive() ? prompt(:prompt) : prompt(:draw);","title":"Setup"},{"category":"section","location":"generated/examples/logistic-sparse/#Data","page":"Sparse logistic regression with POGM","text":"Generate synthetic data from two classes\n\nif !@isdefined(yy)\n    seed!(0)\n    n0 = 60\n    n1 = 50\n    mu0 = [-1, 1]\n    mu1 = [1, -1]\n    v0 = mu0 .+ randn(2,n0) # class -1\n    v1 = mu1 .+ randn(2,n1) # class 1\n    nex = 0\n    if true # 2017-01-18\n        nex = 4 # extra dim (beyond the 2 shown) to make \"larger scale\"\n        v0 = [v0; rand(nex,n0)] # (2+nex, n0)\n        v1 = [v1; rand(nex,n1)] # (2+nex, n1)\n    end\n    v0 = [v0; ones(1,n0)] # (npar, n0) training data - with intercept\n    v1 = [v1; ones(1,n1)] # (npar, n1) training data - with intercept\n    M = n0 + n1 # how many samples\n    yy = [-ones(Int, n0); ones(Int, n1)] # (M) labels\n    vv = [v0 v1] # (npar, M) training data - with intercept\n    npar = 3 + nex # unknown parameters\nend;\n\nScatter plot and initial decision boundary\n\nif !@isdefined(ps)\n    x0 = [-1; 3; rand(nex); 5]\n    v1p = range(-1,1,101) * 4\n    v2p_fun(x) = @. (-x[end] - x[1] * v1p) / x[2]\n\n    ps = plot(aspect_ratio = 1, size = (550, 500), legend = :topright,\n     xaxis = (L\"v_1\", (-4, 4), [-4 -1 0 1 4]),\n     yaxis = (L\"v_2\", (-4, 4), [-4 -1 0 1 4]),\n    )\n    plot!(v1p, v2p_fun(x0), color=:red, label=\"initial\")\n    plot!(v1p, v1p, color=:yellow, label=\"ideal\")\n    alpha = 0.7\n    scatter!(v0[1,:], v0[2,:], color = :green; alpha)\n    scatter!(v1[1,:], v1[2,:], color = :blue, marker = :square; alpha)\nend\nps\nPlot{Plots.GRBackend() n=4}\n\nprompt()\n\n\n\"\"\"\n    model = model_setup(data, label, reg)\nSee `logistic_sparse` for arguments.\n\"\"\"\nfunction model_setup(data, label, reg)\n    all(in((+1,-1)), label) || throw(ArgumentError(\"label not ±1\"))\n\n    #=\n    The gradient of the data-fit part of the cost function is\n    ``∇ f(x) = A' \\dot{h}.(A x)``,\n    and its Lipschitz constant is\n    ``‖A‖₂² / 4``.\n    =#\n\n    pot(t) = log(1 + exp(-t)) # logistic\n    dpot(t) = -1 / (exp(t) + 1)\n    wpot0 = 1/4 # logistic curvature maximum\n    tmp = data * data' # (npar, npar) covariance\n    tmp = eigvals(tmp)\n    f_L = maximum(tmp) * wpot0 # Lipschitz\n\n    A = label .* data' # M × N matrix of features times labels\n\n    # Cost function\n    f_cost(x::AbstractVector) = sum(pot, A * x)\n    f_grad(x) = A' * dpot.(A * x) # gradient of F\n    F_cost(x::AbstractVector) = f_cost(x) + reg * sum(abs, @view x[1:(end-1)])\n    F_cost(x::AbstractMatrix) = F_cost.(eachcol(x)) # to handle arrays\n\n    # proximal operator\n    soft(z,c) = sign(z) * max(abs(z) - c, 0) # soft thresholding\n    g_prox(z, c) = [soft.((@view z[1:(end-1)]), reg * c); z[end]]\n\n    # subgradient of overall cost function (used for QN)\n    F_grad(x) = f_grad(x) + reg * [sign.(@view x[1:(end-1)]); 0]\n\n    return (; f_cost, f_grad, f_L, g_prox, F_cost, F_grad)\nend\n\n\n\"\"\"\n   xh = logistic_sparse(data, label, reg)\n\nPerform sparse logistic regression\nfor binary `label`s\nby minimizing the cost function\n``\nF(x) = f(x) + β ‖ x[1:end-1] ‖₁,\nf(x) = 1_M' h.(A x)\n``\nwhere\n``h(z) = log(1 + e^{-z})``\nis the logistic loss function.\n\nThe regularizer\n``‖ x[1:end-1] ‖₁``\ndoes not penalize the last coefficient of `x`,\nbecause that is the \"bias/intercept\" term.\n\nInternally,\nthis function\nforms the `M × N` matrix of features times labels\n`A = yy .* data'`,\nso the ``m``th row of ``A``\nhas already been multiplied\nby the ``m``th binary class label that is ±1.\n\nAfter optimizing ``x``,\nthe classifier is simply\n``\\text{sign}(⟨v,x⟩)``\nwhere the feature vector ``v``\nincludes the intercept ``1``\nas the last entry.\n\nIn\n- `data` `N × M` where `N` is number of features\n  (including offset aka bias aka intercept ``1``)\n- `label` vector of `M` labels ±1\n- `reg` regularization parameter (β)\n- `fun` see `pogm_restart`; default: count number of nonzero elements of x\n- `niter` # of iterations (default 20)\n\nOption\n- `x0` initial guess of weight vector\n\nOut\n- `xh` minimizer of ``f(x)``\n- `out` output of `pogm_restart`\n\"\"\"\nfunction logistic_sparse(\n    data::AbstractMatrix,\n    label::AbstractVector,\n    reg::Real;\n    how::Symbol = :pogm,\n    x0::AbstractVector = zeros(size(data, 1)),\n    fun::Function = (iter, xk, yk, is_restart) -> count(≠(0), xk),\n    niter::Int = 20,\n)\n    model = model_setup(data, label, reg)\n\n    if how == :pogm\n        xh, out = pogm_restart(x0, model.F_cost, model.f_grad, model.f_L;\n            model.g_prox, fun, niter)\n        return xh, out\n\n    elseif how == :qn\n        opt = Optim.Options(\n            store_trace = false,\n            show_warnings = false,\n            extended_trace = false, # for trace of x\n        )\n        outq = optimize(model.F_cost, model.F_grad, x0, opt; inplace = false)\n        xq = outq.minimizer\n        return xq, outq\n\n    else\n        throw(ArgumentError(\"how $how\"))\n    end\nend;","title":"Data"},{"category":"section","location":"generated/examples/logistic-sparse/#Find-sparse-regression-weights-using-POGM","page":"Sparse logistic regression with POGM","text":"The resulting sparsity pattern is appropriate\n\nif !@isdefined(xpogm)\n    reg = 2^2 # todo: use cross validation to select\n    niter = 20\n    xpogm, pogm_nnz = logistic_sparse(vv, yy, reg; niter, how = :pogm)\nend;","title":"Find sparse regression weights using POGM"},{"category":"section","location":"generated/examples/logistic-sparse/#L-BFGS-Quasi-Newton-optimizer","page":"Sparse logistic regression with POGM","text":"Technically QN is inapplicable because the cost function is not differentiable, but we try it anyway.\n\nThe solution is \"almost, but not quite\" sparse, so a user-selected threshold would be needed.\n\nif !@isdefined(xqn)\n    xqn, outq = logistic_sparse(vv, yy, reg; niter, how = :qn)\nend;\nxideal = [1; -1; zeros(5)] # due to data generation model\ntable1 = [\"ideal\" \"fitted-POGM\" \"fitted-QN\";\n     xideal round.(xpogm, sigdigits=3) round.(xqn, sigdigits=3)]\n8×3 Matrix{Any}:\n   \"ideal\"    \"fitted-POGM\"    \"fitted-QN\"\n  1.0        1.18             1.2\n -1.0       -1.43            -1.4\n  0.0       -0.0             -2.26e-16\n  0.0       -0.0              3.86e-15\n  0.0       -0.0              2.58e-16\n  0.0        0.0              4.23e-15\n  0.0       -0.474           -0.472\n\nCompare final cost functions (POGM is slightly lower)\n\nmodel = model_setup(vv, yy, reg)\ntablem = [model.F_cost(xpogm), model.F_cost(xqn)]\n2-element Vector{Float64}:\n 31.705574735254046\n 31.710420260827853\n\nPlot decision boundaries\n\nif true\n    xh = xpogm\n    psh = deepcopy(ps)\n    v2p = @. (-xh[end] - xh[1] * v1p) / xh[2]\n    plot!(psh, v1p, v2p, color = :magenta, label = \"final\")\nend\npsh\nPlot{Plots.GRBackend() n=5}\n\nprompt()\n\nRerun while saving iterates for plotting\n\nif true\n    fun = (iter, xk, yk, is_restart) -> xk\n    _, outp = logistic_sparse(vv, yy, reg; niter, how = :pogm, fun)\nend;\n\nPlot iterates; convergence is quite fast here\n\nxps = hcat(outp...)'\nppi = plot(0:niter, xps, marker=:dot, # label = \"POGM\",\n    xlabel=\"iteration\", yaxis = (\"parameter\", (-1.6, 1.6), -1:1) )\nplot!(twinx(), 0:niter, Int.(pogm_nnz), color=:black, marker=:square,\n    ylabel=\"# nonzero\", markersize = 2)\nscatter!(fill(niter, npar), xqn, marker = :star, color = :gray)\nPlot{Plots.GRBackend() n=9}\n\nprompt()\n\nPlot cost\n\nppc = plot(xaxis = (\"iteration\", (0,10), 0:2:10),\n    yaxis = (\"Cost function\",), widen=true)\nscatter!(0:niter, model.F_cost(xps'), label = \"POGM\",)\nPlot{Plots.GRBackend() n=1}\n\nprompt()","title":"L-BFGS Quasi-Newton optimizer"},{"category":"section","location":"generated/examples/logistic-sparse/#Plot-1D-separation","page":"Sparse logistic regression with POGM","text":"with accuracy labels\n\nfunction _hist(x, title)\n    inprod0 = v0' * x\n    inprod1 = v1' * x\n    accuracy0 = round(count(<(0), inprod0) / n0 * 100, digits=1)\n    accuracy1 = round(count(>(0), inprod1) / n1 * 100, digits=1)\n\n    p = plot(xaxis=(\"⟨x,v⟩\",); title)\n    bins = -12:12\n    alpha = 0.5\n    histogram!(inprod0; alpha, bins, color = :green, linecolor = :green,\n     label = \"class 0: $accuracy0%\")\n    histogram!(inprod1; alpha, bins, color = :blue, linecolor = :blue,\n     label = \"class 1: $accuracy1%\")\n    return p\nend\np1h = _hist(xh, \"POGM\")\np1q = _hist(xqn, \"QN\")\np1i = _hist(xideal, \"Ideal\")\np1 = plot(p1h, p1q, p1i, layout=(3,1), size=(600,800))\nPlot{Plots.GRBackend() n=6}\n\n\n\nThis page was generated using Literate.jl.","title":"Plot 1D separation"},{"category":"section","location":"methods/#Methods-list","page":"-","text":"","title":"Methods list"},{"category":"section","location":"methods/#Methods-usage","page":"-","text":"","title":"Methods usage"},{"category":"section","location":"#EECS553demo.jl-Documentation","page":"Home","text":"","title":"EECS553demo.jl Documentation"},{"category":"section","location":"#Overview","page":"Home","text":"This Julia package (EECS553demo) provides demos.\n\nSee the \"Examples\" for details.","title":"Overview"}]
}
